
when an attempt starts. the server will check , if with the same user id any attempt is there with submitted ==0.
If yes , then we will load that attempt, and will start from the unattempted question with timer = time given-time used.
If no, then we will look up in the db where the question.topicId == quiz.topicId, order by random and limit = quiz.noOfQuestions
        then we will load all of them with attempt id and a few values to the attempt+question asked table.

First lets create basic DTOs
